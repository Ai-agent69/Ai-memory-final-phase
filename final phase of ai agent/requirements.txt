# Hybrid Agentic Memory System — Runtime Dependencies
# Python >= 3.10

# Ollama Python client — communicates with local Ollama service
ollama>=0.6.2

# MongoDB driver
pymongo>=4.18.1

# HTTP client (used by ollama package and general utility)
requests>=2.32.5

# Development / testing (not required at runtime)
pytest>=9.1.1
pytest-asyncio>=1.4.0
